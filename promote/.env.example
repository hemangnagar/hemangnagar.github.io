# Copy to .env (same folder) and paste the four values from the X Developer
# Console -> your app -> Keys & Tokens. .env is gitignored; never commit it.
X_API_KEY=            # Consumer Key
X_API_SECRET=         # Consumer Secret
X_ACCESS_TOKEN=       # Access Token   (must be generated with Read AND Write)
X_ACCESS_SECRET=      # Access Token Secret
